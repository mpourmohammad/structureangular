import { JalalidatetimePipe } from './jalalidatetime.pipe';

describe('JalalidatetimePipe', () => {
  it('create an instance', () => {
    const pipe = new JalalidatetimePipe();
    expect(pipe).toBeTruthy();
  });
});
